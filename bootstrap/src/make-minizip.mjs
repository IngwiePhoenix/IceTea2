let minizip = {
    src: join(pathSep, [third_party, "minizip"]),
    lzma: join(pathSep, [third_party, "minizip", "lib", "lzma"]),
    brg: join(pathSep, [third_party, "minizip", "lib", "brg"])
};

function make() {}